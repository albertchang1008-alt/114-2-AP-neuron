rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function signedIn() {
      return request.auth != null;
    }

    function hasStudentId() {
      return signedIn()
        && request.resource.data.studentId is string
        && request.resource.data.studentId.size() > 0;
    }

    match /system/{docId} {
      allow read: if true;
      allow write: if false;
    }

    match /questions/{questionId} {
      allow read: if true;
      allow write: if false;
    }

    match /rankingCaches/{docId} {
      allow read: if true;
      allow write: if false;
    }

    match /students/{studentId} {
      allow read, write: if false;
    }

    match /answerBatches/{batchId} {
      allow create: if hasStudentId();
      allow read: if true;
      allow update, delete: if false;
    }

    match /answerDetails/{detailId} {
      allow create: if hasStudentId();
      allow read, update, delete: if false;
    }

    match /studentProgress/{progressId} {
      allow create, update: if hasStudentId();
      allow read: if true;
      allow delete: if false;
    }

    match /wrongQuestions/{wrongId} {
      allow create, update: if hasStudentId();
      allow read: if signedIn()
        && resource.data.studentId is string
        && resource.data.studentId.size() > 0;
      allow delete: if false;
    }

    match /studentAttempts/{studentId} {
      allow read: if true;
      allow write: if false;
    }

    match /loginStates/{studentId} {
      allow read: if signedIn();
      allow create, update: if signedIn()
        && request.resource.data.studentId == studentId
        && request.resource.data.token is string
        && request.resource.data.token.size() > 0;
      allow delete: if false;
    }

    match /offlineQueue/{queueId} {
      allow create: if hasStudentId();
      allow read, update, delete: if false;
    }
  }
}
